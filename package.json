{
  "name": "restful-booker-test",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --timeout 10000"
  },
  "dependencies": {
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "supertest": "^7.1.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  }
}